## Запуск проекта

1. Создать .env файл с переменными:
  ```bash
  POSTGRES_USER
  POSTGRES_DB_PASSWORD
  POSTGRES_DB
  BOT_TOKEN
  ```
   
2. Запустить контейнер:
   ```bash
   docker-compose up --build
   ```
